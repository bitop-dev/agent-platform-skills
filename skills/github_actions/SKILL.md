---
name: github_actions
version: 1.0.0
description: "GitHub Actions — list workflow runs, trigger dispatches, and fetch logs. Use for CI/CD monitoring and management."
author: platform-team
tags: [github, ci, actions, workflows]
emoji: "⚡"
always: false
runtime: wasm
requires: {}
---
# GitHub Actions
Monitor and manage GitHub Actions workflows. List runs, trigger dispatches, fetch logs.

## Required Credentials
- `GITHUB_TOKEN` — GitHub PAT with `actions` scope
